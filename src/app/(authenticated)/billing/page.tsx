import { UnderConstruction } from '../_placeholder/under-construction';

export default function BillingPage(): JSX.Element {
  return (
    <UnderConstruction
      title="Financeiro"
      subtitle="Boletos, recebíveis e comissionamento"
      breadcrumbs={[{ label: 'Dashboard', href: '/dashboard' }, { label: 'Financeiro' }]}
    />
  );
}
