import { UnderConstruction } from '../_placeholder/under-construction';

export default function InstallationsPage(): JSX.Element {
  return (
    <UnderConstruction
      title="Minhas Instalações"
      breadcrumbs={[{ label: 'Dashboard', href: '/dashboard' }, { label: 'Instalações' }]}
    />
  );
}
