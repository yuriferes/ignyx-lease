import { UnderConstruction } from '../../_placeholder/under-construction';

export default function MyBillingPage(): JSX.Element {
  return (
    <UnderConstruction
      title="Boletos"
      breadcrumbs={[{ label: 'Dashboard', href: '/dashboard' }, { label: 'Boletos' }]}
    />
  );
}
