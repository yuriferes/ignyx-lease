import { UnderConstruction } from '../../_placeholder/under-construction';

export default function MySupportPage(): JSX.Element {
  return (
    <UnderConstruction
      title="Chamados"
      breadcrumbs={[{ label: 'Dashboard', href: '/dashboard' }, { label: 'Chamados' }]}
    />
  );
}
