import { UnderConstruction } from '../_placeholder/under-construction';

export default function RepresentativesPage(): JSX.Element {
  return (
    <UnderConstruction
      title="Meus Representantes"
      breadcrumbs={[{ label: 'Dashboard', href: '/dashboard' }, { label: 'Representantes' }]}
    />
  );
}
